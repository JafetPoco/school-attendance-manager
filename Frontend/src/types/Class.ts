export interface ClassRequest {
    level: string;
    grade: string;
    section: string;
}

export interface ClassResponse {
    id: number;
    name: string;

}

export interface ClassFullInfoResponse {
    id: number;
    level: string;
    grade: string;
    section: string;
}